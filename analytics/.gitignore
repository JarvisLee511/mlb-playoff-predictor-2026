# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# DuckDB warehouse (regenerated by `dbt build`)
warehouse/*.duckdb
warehouse/*.duckdb.wal

# Evidence.dev
evidence/node_modules/
evidence/.evidence/
evidence/build/
evidence/.svelte-kit/

# Tableau extract working files
tableau/*.hyper
