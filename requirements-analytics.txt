# Analytics-engineering layer (analytics/). Kept separate from requirements-ci.txt so the
# core prediction pipeline install stays lean. dbt builds + tests the DuckDB warehouse.
# dbt-core is pinned too: it arrives as a transitive dependency, and letting it float
# means a future release can turn today's deprecation warnings into errors and quietly
# redden the daily run.
dbt-core==1.12.0
dbt-duckdb==1.10.1
